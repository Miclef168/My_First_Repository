// Create the addNumbers function here.
function addNumbers() {
    var firstNumber=2;
    var secondNumber=3;
    var firstSum= firstNumber + secondNumber;
    console.log(firstSum);
}
addNumbers();
// Create the stringConcat function here.
function stringConcat () {
    var firstFood='taco';
    var firstAnimal='cat';
    var wordCombo=firstFood + ' ' + firstAnimal;
    alert(wordCombo);
}
stringConcat();